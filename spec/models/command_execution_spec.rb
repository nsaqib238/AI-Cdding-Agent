require 'rails_helper'

RSpec.describe CommandExecution, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:command_execution)
  #   expect(record).to be_persisted
  # end
end
